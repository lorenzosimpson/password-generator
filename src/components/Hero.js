import React from 'react';

function Hero(props) {
    return (
        <div className='hero'> 
           <h1>Generate a pseudo-random password</h1> 
           <p>Use our online password generator to generate a password.</p>
        </div>
    );
}

export default Hero;